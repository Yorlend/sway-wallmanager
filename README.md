# sway-wallmanager

A simple solution for managing wallpapers in sway

1. wallmanagerd keeps track of wallpaper symlink changes && initially loads wallpaper on sway start (if exec wallmanagerd is set in sway config)
2. sway-wallmanager is a simple tui frontend that allows to change wallpaper symlink

